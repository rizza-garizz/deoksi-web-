{
  "name": "deoksi-web",
  "version": "1.0.0",
  "type": "module",
  "main": "script.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:env": "node api/_lib/check-env.mjs",
    "docker:up": "docker compose up --build",
    "docker:up:dev": "docker compose --profile dev up --build",
    "docker:init-db": "docker compose --profile init up db-init",
    "docker:config": "docker compose config",
    "docker:smoke": "node scripts/docker-smoke-check.mjs",
    "docker:seed:verify": "node scripts/docker-seed-verify.mjs",
    "docker:ready": "node scripts/docker-readiness-check.mjs",
    "docker:seed:report": "node scripts/docker-seed-report.mjs",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f app",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "vite": "^6.4.2",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "bcryptjs": "^3.0.3",
    "drizzle-orm": "^0.45.2",
    "jose": "^6.2.2"
  }
}
